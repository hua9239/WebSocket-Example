services:
  websocket-chat:
    build:
      context: .
      dockerfile: dockerfile
    container_name: websocket-chat-app
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
